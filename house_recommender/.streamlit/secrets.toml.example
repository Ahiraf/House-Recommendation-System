# Copy this file to `secrets.toml` (same folder) and paste your real key.
# `secrets.toml` is gitignored, so your key is never committed.
# Streamlit auto-loads it and exposes it as st.secrets["OPENAI_API_KEY"].
OPENAI_API_KEY = "sk-your-openai-key-here"
